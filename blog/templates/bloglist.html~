{% extends "base.html" %}
   {% block head %}
          <link rel="stylesheet" type="text/css" href="/blog/css/bloglist.css">
          <title>{{userName}}的博客</title>
   {%endblock %} 
   {%block userinfo%}
       <img src='/blog/pic/default.jpg'>
       <div><a href="/blog/{{userID}}">{{userName}}</a></div>
   {%endblock%}
   {%block content %}
      {%for blog in bloglists %}
         <div class='bloglist'>
           {%ifequal "list" viewmode%}
           <div align="left" class='blogtitle listview'><a href="/blog/{{blog.userID}}/article/{{blog.id}}">{{blog.title}}</a></div>
           <div class='blogabs'>{{blog.body|truncatewords:"5"|safe}}</div>
           <div class='bloginfo listview'>
             <span id='time' class='headitem'>{{blog.timestamp|date:"Y-m-d H:i:s" }}</span>
             <span id='readnum' class='headitem'>阅读:({{blog.readnum}})| </span>
             <span id='commentnum' class='headitem'>评论:({{blog.commentnum}})</span>
           </div>
           {%else%}
             <a href="/blog/{{blog.userID}}/article/{{blog.id}}">{{blog.title}}</a>
             <div class='bloginfo'>
             <span id='time' class='headitem'>{{blog.timestamp|date:"Y-m-d H:i:s" }}</span>
             <span id='readnum' class='headitem'>阅读:({{blog.readnum}})| </span>
             <span id='commentnum' class='headitem'>评论({{blog.commentnum}})</span></div>
           {%endifequal%}
         </div>
      {% endfor %}
    {% endblock %}
    {%block categorydiv%}
          {%for cate in category %}
               <div><a href="/blog/{{cate.userID}}/category/{{cate.name}}">{{cate.name}}</a><span>({{cate.num}})</span></div>
          {% endfor %}
    {% endblock %}
